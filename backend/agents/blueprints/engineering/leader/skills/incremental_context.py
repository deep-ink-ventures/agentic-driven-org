NAME = "Incremental Context"
DESCRIPTION = "Store and retrieve context summaries per codebase area (e.g. backend/agents/, frontend/components/). Avoids re-discovering patterns across stateless workflow runs. Entries older than 7 days are dropped."
