# AgentDriven Landing Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a single-page marketing landing page for agentdriven.org with waitlist signup via SendGrid.

**Architecture:** Standalone Next.js 15 app in `landing-page/` at monorepo root. App router with one API route for waitlist. All components are custom — no UI library. Design will be polished via the frontend-design skill after all functional code is in place.

**Tech Stack:** Next.js 15, TypeScript, Tailwind CSS 4, SendGrid Marketing Contacts API

**Spec:** `docs/superpowers/specs/2026-04-04-landing-page-design.md`

---

## File Structure

```
landing-page/
├── app/
│   ├── layout.tsx              # Root layout, Inter font, metadata, dark bg
│   ├── page.tsx                # Composes all sections
│   ├── globals.css             # Tailwind imports + custom properties + animations
│   └── api/
│       └── waitlist/
│           └── route.ts        # POST handler → SendGrid
├── components/
│   ├── hero.tsx                # Full-viewport hero with gradient bg
│   ├── value-props.tsx         # Three-column value propositions
│   ├── product.tsx             # Agentic stack description
│   ├── consulting.tsx          # Consulting engagement pitch
│   ├── security.tsx            # BYOC + security
│   ├── quote.tsx               # Placeholder testimonial
│   ├── waitlist.tsx            # Email form + invite-only messaging
│   └── footer.tsx              # Minimal footer
├── .env.local.example          # SENDGRID_API_KEY, SENDGRID_LIST_ID
├── tailwind.config.ts          # Custom colors, fonts
├── next.config.ts              # Minimal config
├── tsconfig.json               # Generated by create-next-app
└── package.json                # Generated by create-next-app
```

---

### Task 1: Scaffold Next.js project

**Files:**
- Create: `landing-page/` (via create-next-app)
- Create: `landing-page/.env.local.example`

- [ ] **Step 1: Create the Next.js app**

Run:
```bash
cd /Users/christianpeters/the-agentic-company
npx create-next-app@latest landing-page --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*" --use-npm
```

When prompted, accept defaults. This creates the full scaffold.

- [ ] **Step 2: Verify it runs**

Run:
```bash
cd /Users/christianpeters/the-agentic-company/landing-page
npm run dev -- --port 3001
```

Expected: Dev server starts on localhost:3001. Kill it after confirming.

- [ ] **Step 3: Create .env.local.example**

Create `landing-page/.env.local.example`:

```
SENDGRID_API_KEY=your-sendgrid-api-key
SENDGRID_LIST_ID=your-sendgrid-list-id
```

- [ ] **Step 4: Commit**

```bash
cd /Users/christianpeters/the-agentic-company
git add landing-page/
git commit -m "feat(landing): scaffold Next.js 15 project"
```

---

### Task 2: Global styles + layout + Tailwind config

**Files:**
- Modify: `landing-page/app/globals.css`
- Modify: `landing-page/app/layout.tsx`
- Modify: `landing-page/tailwind.config.ts`

- [ ] **Step 1: Update tailwind.config.ts**

Replace `landing-page/tailwind.config.ts` with:

```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./app/**/*.{ts,tsx}",
    "./components/**/*.{ts,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        navy: "#0a0e1a",
        "navy-light": "#111827",
        "navy-card": "rgba(17, 24, 39, 0.6)",
        violet: "#6366f1",
        "violet-light": "#8b5cf6",
        amber: "#f59e0b",
        "amber-hover": "#d97706",
        silver: "#94a3b8",
        "silver-light": "#cbd5e1",
      },
      fontFamily: {
        sans: ["Inter", "system-ui", "sans-serif"],
      },
      animation: {
        "gradient-shift": "gradient-shift 8s ease infinite",
        "fade-in": "fade-in 0.6s ease-out forwards",
        "glow-pulse": "glow-pulse 2s ease-in-out infinite",
      },
      keyframes: {
        "gradient-shift": {
          "0%, 100%": { backgroundPosition: "0% 50%" },
          "50%": { backgroundPosition: "100% 50%" },
        },
        "fade-in": {
          from: { opacity: "0", transform: "translateY(20px)" },
          to: { opacity: "1", transform: "translateY(0)" },
        },
        "glow-pulse": {
          "0%, 100%": { boxShadow: "0 0 20px rgba(99, 102, 241, 0.3)" },
          "50%": { boxShadow: "0 0 40px rgba(99, 102, 241, 0.6)" },
        },
      },
    },
  },
  plugins: [],
};

export default config;
```

- [ ] **Step 2: Update globals.css**

Replace `landing-page/app/globals.css` with:

```css
@import "tailwindcss";

@theme {
  --color-navy: #0a0e1a;
  --color-navy-light: #111827;
  --color-navy-card: rgba(17, 24, 39, 0.6);
  --color-violet: #6366f1;
  --color-violet-light: #8b5cf6;
  --color-amber: #f59e0b;
  --color-amber-hover: #d97706;
  --color-silver: #94a3b8;
  --color-silver-light: #cbd5e1;

  --font-sans: "Inter", system-ui, sans-serif;

  --animate-gradient-shift: gradient-shift 8s ease infinite;
  --animate-fade-in: fade-in 0.6s ease-out forwards;
  --animate-glow-pulse: glow-pulse 2s ease-in-out infinite;
}

@keyframes gradient-shift {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

@keyframes fade-in {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes glow-pulse {
  0%, 100% { box-shadow: 0 0 20px rgba(99, 102, 241, 0.3); }
  50% { box-shadow: 0 0 40px rgba(99, 102, 241, 0.6); }
}

html {
  scroll-behavior: smooth;
}

body {
  background-color: var(--color-navy);
  color: white;
}

::selection {
  background-color: rgba(99, 102, 241, 0.4);
  color: white;
}
```

- [ ] **Step 3: Update layout.tsx**

Replace `landing-page/app/layout.tsx` with:

```tsx
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "AgentDriven — Agentic AI Infrastructure & Consulting",
  description:
    "Deploy autonomous AI agents on your own cloud. AgentDriven builds the agentic stack and integrates it into your business. Currently invite-only.",
  openGraph: {
    title: "AgentDriven — Agentic AI Infrastructure & Consulting",
    description:
      "Deploy autonomous AI agents on your own cloud. AgentDriven builds the agentic stack and integrates it into your business.",
    url: "https://agentdriven.org",
    siteName: "AgentDriven",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.className}>
      <body className="antialiased">{children}</body>
    </html>
  );
}
```

- [ ] **Step 4: Verify styles load**

Run: `cd /Users/christianpeters/the-agentic-company/landing-page && npm run dev -- --port 3001`
Expected: Page loads with dark navy background, Inter font.

- [ ] **Step 5: Commit**

```bash
cd /Users/christianpeters/the-agentic-company
git add landing-page/app/globals.css landing-page/app/layout.tsx landing-page/tailwind.config.ts
git commit -m "feat(landing): global styles, layout, tailwind config"
```

---

### Task 3: Hero component

**Files:**
- Create: `landing-page/components/hero.tsx`

- [ ] **Step 1: Create hero.tsx**

```tsx
"use client";

export default function Hero() {
  return (
    <section className="relative min-h-screen flex items-center justify-center overflow-hidden">
      {/* Animated gradient background */}
      <div
        className="absolute inset-0 animate-gradient-shift"
        style={{
          background:
            "linear-gradient(135deg, #0a0e1a 0%, #1e1b4b 25%, #312e81 50%, #1e1b4b 75%, #0a0e1a 100%)",
          backgroundSize: "400% 400%",
        }}
      />

      {/* Gradient overlay for depth */}
      <div className="absolute inset-0 bg-gradient-to-b from-transparent via-transparent to-navy" />

      <div className="relative z-10 max-w-4xl mx-auto px-6 text-center">
        <h1 className="text-5xl md:text-7xl font-bold tracking-tight mb-6">
          Your company,{" "}
          <span className="bg-gradient-to-r from-violet to-violet-light bg-clip-text text-transparent">
            run by agents.
          </span>
        </h1>

        <p className="text-lg md:text-xl text-silver max-w-2xl mx-auto mb-10 leading-relaxed">
          AgentDriven builds agentic AI infrastructure and embeds it into your
          organization. Autonomous agents that handle departments, execute
          workflows, and make decisions — deployed on your own cloud.
        </p>

        <a
          href="#waitlist"
          className="inline-block bg-amber hover:bg-amber-hover text-navy font-semibold px-8 py-4 rounded-lg text-lg transition-all duration-200 hover:scale-105"
        >
          Get early access
        </a>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Commit**

```bash
cd /Users/christianpeters/the-agentic-company
git add landing-page/components/hero.tsx
git commit -m "feat(landing): hero component with animated gradient"
```

---

### Task 4: Value Props component

**Files:**
- Create: `landing-page/components/value-props.tsx`

- [ ] **Step 1: Create value-props.tsx**

```tsx
"use client";

import { useEffect, useRef, useState } from "react";

const props = [
  {
    title: "Agentic AI software",
    description:
      "Not chatbots. Not copilots. Autonomous agents that run departments, execute workflows, and take action. We built the stack. You deploy it.",
  },
  {
    title: "Deep integration consulting",
    description:
      "We don't hand you software and disappear. We embed in your org, configure agents for your specific operations, and push you to the current state of the art.",
  },
  {
    title: "Replace legacy, not people",
    description:
      "Kill the spreadsheets, the manual handoffs, the processes nobody remembers why they exist. Upskill your team. Modernize how your company actually works.",
  },
];

export default function ValueProps() {
  const ref = useRef<HTMLDivElement>(null);
  const [visible, setVisible] = useState(false);

  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) setVisible(true);
      },
      { threshold: 0.2 }
    );
    if (ref.current) observer.observe(ref.current);
    return () => observer.disconnect();
  }, []);

  return (
    <section ref={ref} className="py-32 px-6">
      <div className="max-w-6xl mx-auto grid md:grid-cols-3 gap-12">
        {props.map((prop, i) => (
          <div
            key={prop.title}
            className={`${
              visible ? "animate-fade-in" : "opacity-0"
            }`}
            style={{ animationDelay: `${i * 150}ms` }}
          >
            <h3 className="text-xl font-bold mb-4 text-white">{prop.title}</h3>
            <p className="text-silver leading-relaxed">{prop.description}</p>
          </div>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Commit**

```bash
cd /Users/christianpeters/the-agentic-company
git add landing-page/components/value-props.tsx
git commit -m "feat(landing): value props component with scroll animation"
```

---

### Task 5: Product component

**Files:**
- Create: `landing-page/components/product.tsx`

- [ ] **Step 1: Create product.tsx**

```tsx
"use client";

import { useEffect, useRef, useState } from "react";

export default function Product() {
  const ref = useRef<HTMLDivElement>(null);
  const [visible, setVisible] = useState(false);

  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) setVisible(true);
      },
      { threshold: 0.2 }
    );
    if (ref.current) observer.observe(ref.current);
    return () => observer.disconnect();
  }, []);

  return (
    <section ref={ref} className="py-32 px-6">
      <div
        className={`max-w-3xl mx-auto ${visible ? "animate-fade-in" : "opacity-0"}`}
      >
        <h2 className="text-3xl md:text-5xl font-bold tracking-tight mb-8">
          An agentic stack you actually{" "}
          <span className="bg-gradient-to-r from-violet to-violet-light bg-clip-text text-transparent">
            own.
          </span>
        </h2>

        <div className="space-y-6 text-lg text-silver leading-relaxed">
          <p>
            AgentDriven is a full agentic AI platform. Departments staffed by AI
            agents. Configurable workflows that adapt to your business logic.
            Automated actions across your entire operation.
          </p>
          <p>
            It runs on your cloud. You control the infrastructure, the data, and
            the agents. No black boxes. No mystery APIs. Every decision is
            traceable, every workflow is yours to modify.
          </p>
          <p className="text-white font-medium">
            This isn&apos;t another SaaS you subscribe to. It&apos;s infrastructure you
            own.
          </p>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Commit**

```bash
cd /Users/christianpeters/the-agentic-company
git add landing-page/components/product.tsx
git commit -m "feat(landing): product section component"
```

---

### Task 6: Consulting component

**Files:**
- Create: `landing-page/components/consulting.tsx`

- [ ] **Step 1: Create consulting.tsx**

```tsx
"use client";

import { useEffect, useRef, useState } from "react";

export default function Consulting() {
  const ref = useRef<HTMLDivElement>(null);
  const [visible, setVisible] = useState(false);

  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) setVisible(true);
      },
      { threshold: 0.2 }
    );
    if (ref.current) observer.observe(ref.current);
    return () => observer.disconnect();
  }, []);

  return (
    <section ref={ref} className="py-32 px-6">
      <div
        className={`max-w-3xl mx-auto ${visible ? "animate-fade-in" : "opacity-0"}`}
      >
        <h2 className="text-3xl md:text-5xl font-bold tracking-tight mb-8">
          We don&apos;t just sell you software.
        </h2>

        <div className="space-y-6 text-lg text-silver leading-relaxed">
          <p>
            Most AI consultancies give you a slide deck and a proof of concept.
            We deploy production systems.
          </p>
          <p>
            AgentDriven consultants embed in your organization. We map your
            operations, configure agentic workflows, and ship working
            infrastructure. Focused engagements, technical depth, measurable
            outcomes.
          </p>
          <p className="text-white font-medium">
            We make your company agentic-first. Then you run it.
          </p>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Commit**

```bash
cd /Users/christianpeters/the-agentic-company
git add landing-page/components/consulting.tsx
git commit -m "feat(landing): consulting section component"
```

---

### Task 7: Security & BYOC component

**Files:**
- Create: `landing-page/components/security.tsx`

- [ ] **Step 1: Create security.tsx**

```tsx
"use client";

import { useEffect, useRef, useState } from "react";

export default function Security() {
  const ref = useRef<HTMLDivElement>(null);
  const [visible, setVisible] = useState(false);

  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) setVisible(true);
      },
      { threshold: 0.2 }
    );
    if (ref.current) observer.observe(ref.current);
    return () => observer.disconnect();
  }, []);

  return (
    <section ref={ref} className="py-32 px-6">
      <div
        className={`max-w-3xl mx-auto ${visible ? "animate-fade-in" : "opacity-0"}`}
      >
        <h2 className="text-3xl md:text-5xl font-bold tracking-tight mb-8">
          Your cloud. Your data.{" "}
          <span className="bg-gradient-to-r from-violet to-violet-light bg-clip-text text-transparent">
            Your keys.
          </span>
        </h2>

        <div className="space-y-6 text-lg text-silver leading-relaxed">
          <p>
            Bring your own cloud — GCP, AWS, or Azure. Your data never touches
            our servers. API keys live in your secret manager. Agents run in your
            VPC.
          </p>
          <p className="text-white font-medium">
            No vendor lock-in. No data residency surprises. Security-first
            architecture from day one.
          </p>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Commit**

```bash
cd /Users/christianpeters/the-agentic-company
git add landing-page/components/security.tsx
git commit -m "feat(landing): security and BYOC section"
```

---

### Task 8: Quote component (placeholder)

**Files:**
- Create: `landing-page/components/quote.tsx`

- [ ] **Step 1: Create quote.tsx**

```tsx
"use client";

import { useEffect, useRef, useState } from "react";

export default function Quote() {
  const ref = useRef<HTMLDivElement>(null);
  const [visible, setVisible] = useState(false);

  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) setVisible(true);
      },
      { threshold: 0.2 }
    );
    if (ref.current) observer.observe(ref.current);
    return () => observer.disconnect();
  }, []);

  return (
    <section ref={ref} className="py-32 px-6">
      <div
        className={`max-w-3xl mx-auto ${visible ? "animate-fade-in" : "opacity-0"}`}
      >
        <div className="border-l-4 border-violet pl-8">
          <blockquote className="text-2xl md:text-3xl font-light text-silver-light leading-relaxed italic">
            &ldquo;Placeholder for client testimonial. Replace this with a real
            quote.&rdquo;
          </blockquote>
          <p className="mt-6 text-silver">
            <span className="text-white font-medium">Name Surname</span>
            {" — "}Title, Company
          </p>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Commit**

```bash
cd /Users/christianpeters/the-agentic-company
git add landing-page/components/quote.tsx
git commit -m "feat(landing): quote placeholder component"
```

---

### Task 9: Waitlist component + API route

**Files:**
- Create: `landing-page/components/waitlist.tsx`
- Create: `landing-page/app/api/waitlist/route.ts`

- [ ] **Step 1: Create the API route**

Create `landing-page/app/api/waitlist/route.ts`:

```typescript
import { NextResponse } from "next/server";

export async function POST(request: Request) {
  const { email } = await request.json();

  if (!email || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
    return NextResponse.json(
      { error: "Please enter a valid email address." },
      { status: 400 }
    );
  }

  const apiKey = process.env.SENDGRID_API_KEY;
  const listId = process.env.SENDGRID_LIST_ID;

  if (!apiKey || !listId) {
    console.error("Missing SENDGRID_API_KEY or SENDGRID_LIST_ID");
    return NextResponse.json(
      { error: "Waitlist is temporarily unavailable." },
      { status: 500 }
    );
  }

  const response = await fetch(
    "https://api.sendgrid.com/v3/marketing/contacts",
    {
      method: "PUT",
      headers: {
        Authorization: `Bearer ${apiKey}`,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        list_ids: [listId],
        contacts: [{ email }],
      }),
    }
  );

  if (!response.ok) {
    const error = await response.text();
    console.error("SendGrid error:", error);
    return NextResponse.json(
      { error: "Something went wrong. Please try again." },
      { status: 500 }
    );
  }

  return NextResponse.json({ success: true });
}
```

- [ ] **Step 2: Create the waitlist component**

Create `landing-page/components/waitlist.tsx`:

```tsx
"use client";

import { FormEvent, useEffect, useRef, useState } from "react";

export default function Waitlist() {
  const ref = useRef<HTMLDivElement>(null);
  const [visible, setVisible] = useState(false);
  const [email, setEmail] = useState("");
  const [status, setStatus] = useState<"idle" | "loading" | "success" | "error">("idle");
  const [errorMessage, setErrorMessage] = useState("");

  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) setVisible(true);
      },
      { threshold: 0.2 }
    );
    if (ref.current) observer.observe(ref.current);
    return () => observer.disconnect();
  }, []);

  async function handleSubmit(e: FormEvent) {
    e.preventDefault();
    setStatus("loading");
    setErrorMessage("");

    const res = await fetch("/api/waitlist", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ email }),
    });

    const data = await res.json();

    if (data.success) {
      setStatus("success");
      setEmail("");
    } else {
      setStatus("error");
      setErrorMessage(data.error || "Something went wrong.");
    }
  }

  return (
    <section id="waitlist" ref={ref} className="py-32 px-6">
      <div
        className={`max-w-xl mx-auto text-center ${visible ? "animate-fade-in" : "opacity-0"}`}
      >
        <h2 className="text-3xl md:text-5xl font-bold tracking-tight mb-4">
          Currently invite-only.
        </h2>
        <p className="text-lg text-silver mb-10">
          Due to high demand, we&apos;re onboarding clients in batches. Join the
          waitlist and we&apos;ll reach out when your spot opens.
        </p>

        {status === "success" ? (
          <div className="bg-navy-card backdrop-blur-sm border border-violet/30 rounded-xl p-8">
            <p className="text-xl text-white font-medium">
              You&apos;re on the list. We&apos;ll be in touch.
            </p>
          </div>
        ) : (
          <form onSubmit={handleSubmit} className="flex flex-col sm:flex-row gap-4">
            <input
              type="email"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              placeholder="your@email.com"
              required
              className="flex-1 bg-navy-light border border-silver/20 rounded-lg px-6 py-4 text-white placeholder:text-silver/50 focus:outline-none focus:border-violet focus:shadow-[0_0_20px_rgba(99,102,241,0.3)] transition-all duration-200"
            />
            <button
              type="submit"
              disabled={status === "loading"}
              className="bg-amber hover:bg-amber-hover disabled:opacity-50 text-navy font-semibold px-8 py-4 rounded-lg text-lg transition-all duration-200 hover:scale-105 whitespace-nowrap"
            >
              {status === "loading" ? "Joining..." : "Join the waitlist"}
            </button>
          </form>
        )}

        {status === "error" && (
          <p className="mt-4 text-red-400 text-sm">{errorMessage}</p>
        )}
      </div>
    </section>
  );
}
```

- [ ] **Step 3: Commit**

```bash
cd /Users/christianpeters/the-agentic-company
git add landing-page/components/waitlist.tsx landing-page/app/api/waitlist/route.ts
git commit -m "feat(landing): waitlist component + SendGrid API route"
```

---

### Task 10: Footer component

**Files:**
- Create: `landing-page/components/footer.tsx`

- [ ] **Step 1: Create footer.tsx**

```tsx
export default function Footer() {
  return (
    <footer className="py-12 px-6 border-t border-silver/10">
      <div className="max-w-6xl mx-auto flex flex-col sm:flex-row items-center justify-between gap-4 text-sm text-silver">
        <span className="font-medium text-white">AgentDriven</span>
        <span>&copy; {new Date().getFullYear()} AgentDriven. All rights reserved.</span>
        <a
          href="mailto:hello@agentdriven.org"
          className="hover:text-white transition-colors"
        >
          hello@agentdriven.org
        </a>
      </div>
    </footer>
  );
}
```

- [ ] **Step 2: Commit**

```bash
cd /Users/christianpeters/the-agentic-company
git add landing-page/components/footer.tsx
git commit -m "feat(landing): footer component"
```

---

### Task 11: Compose page.tsx

**Files:**
- Modify: `landing-page/app/page.tsx`

- [ ] **Step 1: Replace page.tsx**

Replace `landing-page/app/page.tsx` with:

```tsx
import Hero from "@/components/hero";
import ValueProps from "@/components/value-props";
import Product from "@/components/product";
import Consulting from "@/components/consulting";
import Security from "@/components/security";
import Quote from "@/components/quote";
import Waitlist from "@/components/waitlist";
import Footer from "@/components/footer";

export default function Home() {
  return (
    <main>
      <Hero />
      <ValueProps />
      <Product />
      <Consulting />
      <Security />
      <Quote />
      <Waitlist />
      <Footer />
    </main>
  );
}
```

- [ ] **Step 2: Verify the full page loads**

Run: `cd /Users/christianpeters/the-agentic-company/landing-page && npm run dev -- --port 3001`
Expected: All 8 sections render, scroll works, gradient animates, waitlist form appears.

- [ ] **Step 3: Commit**

```bash
cd /Users/christianpeters/the-agentic-company
git add landing-page/app/page.tsx
git commit -m "feat(landing): compose all sections into page"
```

---

### Task 12: Design polish pass

**Files:**
- Potentially modify any component or CSS file

- [ ] **Step 1: Run the frontend-design skill**

Invoke the `frontend-design` skill to review and polish the entire landing page. The skill should:
- Open the page at http://localhost:3001
- Review the visual quality of every section
- Improve typography, spacing, animation timing, color balance
- Ensure the glass-morphism, gradient, and glow effects look premium
- Ensure mobile responsiveness works cleanly
- Make the design exceptional — this is the public face of the company

- [ ] **Step 2: Commit design improvements**

```bash
cd /Users/christianpeters/the-agentic-company
git add landing-page/
git commit -m "feat(landing): design polish pass"
```

---

### Task 13: Build verification

- [ ] **Step 1: Run production build**

Run:
```bash
cd /Users/christianpeters/the-agentic-company/landing-page
npm run build
```

Expected: Build succeeds with no errors.

- [ ] **Step 2: Commit any build fixes**

If build fails, fix issues and commit:
```bash
cd /Users/christianpeters/the-agentic-company
git add landing-page/
git commit -m "fix(landing): build fixes"
```
